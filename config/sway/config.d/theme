# Colors
set {
  $cyan			    #689d6a
  $green			  #98971a
  $orange			  #d65d0e
  $pink			    #FF5370
  $purple			  #b16286
  $red			    #bf6374
  $yellow			  #d79921
  $dark         #323732
  $dark         #333333
  $black        #000000
  $white        #ffffff
  $color1       #0c0c0c
  $color2       #333845
  $unfocus      #ffffff40
  $focus        #ffffff90
  $clear        #ffffff00
}

# class                   border    background  text        indicator child-border
client.focused            $dark    $dark        $focus      $dark     $dark
client.focused_inactive   $dark    $dark        $focus      $dark     $dark
client.unfocused          $dark    $dark        $unfocus    $dark     $dark
client.urgent             $dark    $dark        $focus      $dark     $dark
client.placeholder        $dark    $color1      $white      $dark     $dark
client.background         $white

gaps inner 20
gaps outer 10
# gaps inner 0
# gaps outer 0
# smart_gaps on



# Default font
font pango:SourceCodePro Medium 10

# Title format for windows
for_window [shell="xdg_shell"] title_format "%title (%app_id)"
for_window [shell="x_wayland"] title_format "wayland: %class - %title"
for_window [title="."] title_format "%title"

# default_border pixel 0
#
default_border normal 4
# default_border pixel 4
#
# default_border normal 6
# default_border normal 0
# default_floating_border normal 6
# titlebar_border_thickness 0
# titlebar_padding 2 2
# title_align center

# hide_edge_borders smart_no_gaps
hide_edge_borders both
# smart_borders smart
seat seat0 xcursor_theme default 70


# Wallpaper
# output * bg $wallpapers/arch-black.jpg fill
output * bg $wallpapers/arch-gray.jpg fill
# output * bg $wallpapers/orange.jpg fill
# output * bg $wallpapers/leaves.jpg fill

# Resolution & scale swaymsg -t get_outputs
# example: output HDMI-A-1 resolution 1920x1080 position 1920,0
# output eDP-1 scale 1.5
output eDP-1 scale 1.5
#xwayland disable

xwayland scale 2
exec xrdb -merge ~/.Xresources

exec_always {

  gsettings reset org.gnome.desktop.sound theme-name
  gsettings reset org.gnome.desktop.interface {
    cursor-theme
    cursor-size
    document-font-name
    font-name
    gtk-theme
    gtk-color-theme
    icon-theme
    monospace-font-name
    text-scaling-factor
  }

  # gsettings set org.gnome.desktop.interface {
  #   # gtk-theme           Materia-dark
  #   gtk-theme           Nordic
  #   icon-theme          Zafiro
  #   document-font-name  'Noto Sans Regular 10'
  #   font-name           'Noto Sans Regular 10'
  #   # document-font-name  'Open Sans Regular 10'
  #   # font-name           'Open Sans Regular 10'
  #   monospace-font-name 'JetBrainsMono Nerd Font 10'
  #   # text-scaling-factor 1.2
  #   text-scaling-factor 1.0
  #   cursor-size         68
  # }

}

bar swaybar_command waybar

# Behaviour
# focus_follows_mouse     	no
focus_follows_mouse     	yes
focus_wrapping          	no
popup_during_fullscreen 	leave_fullscreen
# mouse_warping          	  output
# mouse_warping          	  container

# bindsym m exec flashfocus
bindcode --release $Super_L exec nc_flash_window
bindcode --release $Super_R exec nc_flash_window
# exec_always flashfocus
# bindsym $Super+n exec flash_window

# bindcode $mod+25 input type:keyboard repeat_delay 999999, workspace back_and_forth
# bindsym --release $mod+w workspace back_and_forth, input type:keyboard repeat delay 500


### Idle configuration
# Example configuration:
#
# exec swayidle -w \
#          timeout 300 'swaylock -f -c 000000' \
#          timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
#          before-sleep 'swaylock -f -c 000000'
#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.
